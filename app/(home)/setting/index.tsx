import { Text, View } from "react-native";

type SettingProps = {};
const Setting = (props: SettingProps) => {
  return (
    <View>
      <Text>Setting</Text>
    </View>
  );
};

export default Setting;

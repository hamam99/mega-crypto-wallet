import { Text, View } from "react-native";

type BarcodeProps = {};
const Barcode = (props: BarcodeProps) => {
  return (
    <View>
      <Text>Barcode</Text>
    </View>
  );
};

export default Barcode;
